# Image AI Inference Workbench

Focused portfolio project for image manipulation + ML framework integration.

## Showcased Skills

- FastAPI REST API in modern Python.
- Image processing pipeline with Pillow.
- TensorFlow and PyTorch runtime integration checks.
- React frontend (functional components + hooks + context).
- CI/CD sample for Azure DevOps.

## Structure

- `api/` FastAPI backend
- `web/` React frontend
- `azure-pipelines.yml`
- `docs/`
